[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry.dependencies]
python = "^3.8"
pandas = "^1.5.3"  # Version que tu utilises
numpy = "^1.21.6"
matplotlib = "^3.4.3"
seaborn = "^0.11.2"
# Ajoute ici toutes les bibliothèques que tu as utilisées avec les versions
